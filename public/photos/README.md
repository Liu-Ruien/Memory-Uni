# Replacing the demo photos

1. Put your image files in this folder.
2. Open `src/data/photos.ts`.
3. Add or edit an item so `src` points to `/photos/your-file.jpg`.

Portrait images around a 3:4 aspect ratio work best in the fan carousel. The gallery can use any aspect ratio by changing `gridAspect`.
